Purpose: Document architectural decisions and patterns for the provided code context.

Scope: Document only the specific code flow, files, endpoints, or features provided in the context.

Location: Save to `docs/architecture/` directory (create if doesn't exist). Update existing file if specified, otherwise create new file with descriptive name.

Content Requirements:
- Architectural decisions with rationale (why specific patterns chosen)
- Service interaction diagrams (ASCII or Mermaid)
- Data flow for key features
- Component responsibilities and boundaries
- Technology choices and trade-offs
- Integration points and dependencies

Format:
- Clear headings for each architectural aspect
- Diagrams where helpful (Mermaid preferred for complex flows)
- Code references with file:line where relevant
- Future maintainer considerations

Output:
- File path where documentation saved
- Brief summary of documented architecture
- Key decisions highlighted
