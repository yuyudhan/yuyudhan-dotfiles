# FilePath: zsh/configs/98-behavior.zsh

# Custom shell behavior modifications (optimized for performance)

# Lightweight blank line spacing (only when needed)
# Uncomment these if you want blank lines, but note they may affect performance
# preexec() { echo }
# precmd() { echo }